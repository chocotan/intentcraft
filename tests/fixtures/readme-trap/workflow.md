# Workflow

The entry receives an updated requirement, then runs the [write-back rule](references/write-back.md).
Execution is delegated to [consumer.js](consumer.js).
Regression coverage lives in [check.js](check.js).
