# Working on Intentcraft

- 用简洁中文写面向人的回复、报告和文档；代码、路径、协议 key 保持原格式。
- 维护四个仅由用户显式调用的 skill：`ic-research`、`ic-prepare`、`ic-do`、`ic-code-review`；开发前审查使用 `ic-prepare review`，不建立自动生命周期引擎。
- 每个 `SKILL.md` 保留 `disable-model-invocation: true` 和明确启动边界；skill 不自动调用下一个 skill。
- 阶段核心边界留在 `SKILL.md`，详细方法放随包参考，引用不启动阶段或扩大授权。产品研究、系统设计、准备审查、验证各自维护方法；前端路由与上游适配集中在 `skills/_shared/frontend-methods.md`，不是新 Skill。分发包含所有引用，不增加命令、解析协议或编排层。
- Agent 正文只保留职责、角色边界、方法输入和交付责任，不复制 IC 阶段规则、具体技能路由或技术清单。个人偏好在用户/项目指引维护，不把本机偏好写进通用包；技能通过名称和运行时位置定位，资源用相对路径。
- `ic-research` 只做有界证据研究；`ic-prepare` 普通模式负责产品/需求/UI/Design/Plan，`grill` 挑战并讨论修订，`review` 只读核验准备成果；`ic-do` 负责授权实现和验证；`ic-code-review` 只审代码变更和测试证据。
- 每个调用先选当前 skill 的主产出；不因调用 `ic-prepare` 自动生成全套产品、UI、Design 和计划，用户明确要求组合时也不填空或扩大授权。
- 阶段采用主文档 `docs/ic-*/YYYY-MM-DD-<topic>.md` 加同主题目录 `docs/ic-*/YYYY-MM-DD-<topic>/` 的专题/证据附件。主会话维护结论、状态和索引；只返回正文的角色由主会话落盘，其他角色也须同时满足角色权限和产物分配。目录由阶段/任务确定，方法参考不另建一套。展示原型放项目内 `design-previews/YYYY-MM-DD-<topic>/`，可丢弃技术实验才放隔离临时目录；代码与测试遵循项目结构。
- `ic-prepare review` 是上述改稿规则的例外：仅写同主题目录内独立的 `review.md` 与 `review-evidence/`，不修改被审准备稿及其索引/状态/时间。审查后停止，只有用户明确要求才能切回普通准备修订。
- 事实由 agent 核实，产品取舍由用户决定；区分自述、文档契约、静态追踪、测试定义、运行观察和推断。“未找到”不写成“没有”。
- 非平凡实现要有相称测试；有界面功能覆盖关键用户旅程和状态的浏览器自动化。未运行或工具不可用必须标明，不把静态提示词检查写成运行保障。
- 主会话持有产品上下文、用户讨论、授权、阶段推进和最终整合。默认直接工作；有价值时按能力委派，不依赖固定类型名或固定流水线。用任务、依据、授权、方法、交付传递有界契约与完整相关约束，不默认让子代理重读/执行整套阶段；子代理不重新访谈用户、不再次委派、不推进阶段。一个工作目录一个写入者，并行写入先安排独立目录。
- 操作权限取用户授权、角色边界、任务范围与宿主限制的交集；Skill、方法或工具可用不增加权限。启动/借用进程与会话时明确负责人和停止条件，只清理自有且不再需要的资源。上述文字不是权限沙箱。
- 完整产品研究对正式比较对象做浏览器实查，有开源必须克隆固定版本并深入源码，分别交架构/UI/数据架构/体验附件；窄 API/代码调查不套全流程。记录许可来源，复用前核对义务与兼容性；安装运行第三方项目须明确授权，不把静态分析写成运行证据。
- 系统级设计由主会话与用户确认，前端角色仅负责专业任务。实施和代码审查共用 `skills/ic-do/references/verification.md`；视觉打磨轮次不豁免功能缺陷或必要验证失败。
- `ic-prepare review`、`ic-code-review` 的被审材料只读；发现问题仅报告，不自动修复、提交或发布。审查独立性取决于上下文，不取决于模式名。
- 本仓库不维护 skill 提示词测试套件；交付前检查 `npm pack --dry-run --json --ignore-scripts` 的分发内容及 `git diff --check`，通过实际会话判断提示词效果。
- 不暂存或发布 `example/`、`.pi/`、凭据和会话文件；优先原生能力、已有工具和最小实现。
