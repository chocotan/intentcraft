# Intentcraft

**先把问题想清楚，再把计划写到能交接。**

Intentcraft 是两个仅由用户显式启动的 AI skills，重点服务产品开发前期：
可信研究、方案讨论、需求收敛、必要原型、技术规划与独立文档审查。
它不是自动开发流水线，不接管普通编码任务，也不要求每个项目走全套流程。

## 两个入口

| Skill | 责任 | 结束时得到什么 |
|---|---|---|
| `intentcraft` | 对一个主题的研究、讨论、需求与计划持续负责 | 有依据的结论、已确认需求或可交接计划 |
| `intentcraft-review` | 独立、只读地审查研究、需求和计划 | 有证据的问题、覆盖缺口与明确结论 |

研究、头脑风暴和计划是同一负责人的工作方法，不是需要用户手动接力的三个 skill。
需求调研遇到路线选择时，会按需调查同类商业项目、同类开源项目、可直接复用的开源库
和仓库已有能力；官网界面可用浏览器查看，公开源码候选会 clone 后做模块、功能、UI、
工作流、架构与技术设计分析。许可证暂不作为需求调研阶段的筛选依据。
两个入口也不自动互相调用。评审、执行、提交和发布都不因写完计划而自动发生。

## 安装与主动触发

### Pi（主要支持的宿主）

```bash
pi install git:github.com/chocotan/intentcraft
```

仅在一个项目安装可加 `-l`；本地试用可执行 `pi install /path/to/intentcraft`。
仓库保持 `private: true` 的 npm 发布保护，使用 Git 或本地包安装，不发布 npm 包。
安装后由 Pi plugin 注册短斜杠命令：

```text
/intentcraft 我想做一个团队知识库，先讨论目标，不要实现。
/intentcraft 只研究这两个竞品的权限与分享工作流，按方案选型深度调查。
/intentcraft 继续 docs/specs/import.md，从上次的待决问题开始。
/intentcraft 需求已确认，请规划后台导入任务；没有前端，不需要原型。
/intentcraft-review 审查 docs/plans/import.md，并核对引用的需求与证据。
```

plugin 命令直接加载包内对应的 `SKILL.md`，附上显式启动说明、文件绝对位置和
相对引用基准；references 相对 skill 目录读取，不相对当前业务项目。
不依赖 Pi 的 skill 命令开关，也不复制资源或改变工作目录。
两个 skill 仍设置 `disable-model-invocation: true`，因此也保留兼容入口：

```text
/skill:intentcraft ...
/skill:intentcraft-review ...
```

兼容入口在支持该字段的 Pi 中不进入模型自动发现的 skill 列表；若关闭了
skill 命令，使用上面的 plugin 命令即可。

短命令目前只转发文本，不保留同次消息的图片附件。带图请求请开启 skill 命令并
直接使用 `/skill:intentcraft` 或 `/skill:intentcraft-review`；不要通过短命令提交图片。

主文件还要求用户点名才启用：普通“帮我开发”“写个计划”不会自动启动它们。
显式启动后的同一主题可以自然继续；完成、取消或切换主题后不常驻生效。
用户可在一次指令中明确授权后续独立审查，但 skill 不自己产生这份授权。

### 其他宿主

每个 `skills/<name>/` 是独立包，可按宿主规范安装或手动加载 `SKILL.md`。
但 `disable-model-invocation` 不是所有宿主都支持的通用强制开关。
**不识别该字段的宿主不要把本包放进自动发现目录**；使用其手动加载能力。
本文不承诺未验证宿主上的主动触发隔离。没有启动指令时，阅读源码不等于执行 skill。

## 不同任务走不同深度

| 请求 | 实际处理 |
|---|---|
| 简单、清楚的改动 | 简要说明目标、改动与验证即可；不强制建档、外部研究、原型或完整计划 |
| “只调研，不设计” | 输出证据与缺口后停止，不擅自写需求 |
| 模糊的新产品 | 研究与讨论交替，必要时用原型或实验解决具体问题 |
| 需求已明确 | 直接进入规划，不重复头脑风暴 |
| 纯后端、CLI、库、数据管道 | 讨论接口、数据、状态和失败路径；默认跳过 UI、浏览器与视觉原型 |
| 包含新 UI/交互 | 根据决策需要制作可操作原型；新产品 UI 在正式实现前需用户看过并批准，除非明确豁免 |
| 一行权限或数据删除改动 | 行数少不代表风险低，仍保留对应的确认与验证 |

本包重点是前期工作。用户要立即编码时，简要交接给宿主正常编码流程并结束塑形；
不借此自动启动另一套开发 skill，也不让后续编码永久继承规划模式。

## 研究不能只读 README

研究先确定问题和深度，再追踪相关链路：入口 → 规则/实现 → 关键引用 →
输出 → 消费者 → 测试或运行观察。对 skill 项目，主文件指向的核心 references
和脚本也属于调查对象。

- README 是定位线索或项目自述，不是实现证明。
- 搜索命中和文件片段不是完整语义；关键分支、被截断内容和相关引用必须补齐。
- 明确区分文档契约、静态实现、测试定义、实际运行和推断。
- “未找到”不写成“没有”；结论不超过已调查范围。
- 选型结论附证据、版本/条件、覆盖缺口及对本项目的适用性。
- 不要求读完整个仓库；要求读完整支撑当前结论的相关链路。

## 产物不重复

沿用项目已有文档。没有既有归宿时，复杂主题默认使用一份主题规格，分开保存
需求与计划内容；研究证据过长才拆报告，原型单独保留并引用。
简单讨论可以只留在聊天中；需要跨会话时，经授权保存最小检查点。
草案、已批准需求、计划就绪、执行授权是不同概念。

改动需求时复核相关计划，不把整份计划无故推倒，也不沿用失效依据。
用户认可方向不等于授权实现、提交、联网发布或接受风险。

## 验证与局限

```bash
npm test
npm pack --dry-run --json
# 可选：本地已安装 Pi 和 Python 3 时验证实际加载/手动触发
python3 tests/check_pi.py
# 关闭 skill 命令且不注册两个目标 skill，只通过 plugin 触发
python3 tests/check_pi.py --plugin-only
```

检查使用 Node.js 20+ 标准库，无运行依赖。测试检查目录、手动触发字段、链接、
关键规则标记与打包白名单，不证明模型必然遵守规则。
可选 Pi 检查使用隔离配置与本地假模型，验证自动发现隐藏、阳性对照、手动命令
展开、绝对位置和相对引用基准，以及短命令空参数与 plugin-only 模式。
它检查实际模型请求，不证明模型会正确读取 reference；不使用用户凭据、不修改
全局配置、不请求外部模型。
行为测试输入和判据见 [tests/scenarios.md](tests/scenarios.md)；
实际执行记录见 [docs/validation.md](docs/validation.md)。

本包没有 hook、权限沙箱或确定性状态机。工具是否可用、浏览器和研究能否运行，
取决于宿主；缺少能力时必须报告降级，不能把提示词约束说成运行时保障。

## 来源与许可

设计取舍参考 CodeStable、Compound Engineering、superpowers 和 bigpowers，
详见 [ATTRIBUTION.md](ATTRIBUTION.md)。新编写内容采用 [MIT](LICENSE)。
本地 `example/` 与会话资料不属于仓库或安装包。
